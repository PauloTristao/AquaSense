@model SensorViewModel
@{
    Layout = "_Layout";
}

<style>
    fieldset {
        padding: 0 10px 10px;
        border: 1px solid #666;
        border-radius: 8px;
        padding-top: 10px;
    }

    legend {
        padding: 2px 4px;
        width: 15%
    }

    .container {
        max-width: 100%;
        width: 50%; 
        margin: 0 auto; 
    }

</style>
<form action="Save" method="post">
    <div class="container">

        <fieldset>
            <legend>Cadastro</legend>
            <input type="hidden" name="Operacao" value="@ViewBag.Operacao" />
            <input asp-for="Id" type="hidden" />

            @if (!ViewData.ModelState.IsValid)
            {
                    <div class="alert alert-danger">
                        <p>Por favor, corrija os seguintes erros:</p>
                        <ul>
                        @foreach (var key in ViewData.ModelState.Keys)
                        {
                            foreach (var error in ViewData.ModelState[key].Errors)
                            {
                                            <li>@error.ErrorMessage</li>
                            }
                        }
                        </ul>
                    </div>
            }

            <label for="Descricao" class="control-label">Descrição</label>
            <input asp-for="Descricao" class="form-control" />
            <span asp-validation-for="Descricao" class="text-danger"></span>
            <br />

            <label for="CodigoFiware" class="control-label">Codigo Fiware</label>
            <input asp-for="CodigoFiware" class="form-control" />
            <span asp-validation-for="CodigoFiware" class="text-danger"></span>
            <br />

            <input type="submit" value="Salvar dados" class="btn btn-info" />
            <a href="javascript:history.back()" class="btn btn-default">Voltar</a>
            <br />
        </fieldset>
    </div>
</form>

